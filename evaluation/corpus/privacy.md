# Local privacy boundary

ContextVault keeps source documents, vector embeddings, and generated answers on the user's machine. During normal use it contacts only the localhost Ollama service and never sends document content to a cloud inference provider.
